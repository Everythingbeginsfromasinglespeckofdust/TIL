//외부 또는 내부 파일을 현재 파일에 포함시킬 수 있는 명령어
//주석
#include<stdio.h>

//실행 파일을 만들어주는 함수.
//이 안에 소스코드를 작성하면 된다.
void main(){
	//컴파일 단축기 : ctrl + F5
	printf("정대한");
}

#include<stdio.h>

void main() {
	int i = 10;
	long l = 10L;
	float f = 3.9F;
	double d = 3.9;
	char c = 'a';
	char* str = "ABC";

	//ctrl + k 눌렀다가 뗀 후 c : 한줄 주석
	//ctrl + k 눌렀다가 뗀 후 u : 주석 해제
	//sizeof(자료형) : 소괄호 안에 작성한 변수 또는 자료형의 크기를 알려주는 함수이다.
	printf("%d\n", sizeof(int));
	printf("%d\n", sizeof(long));
	printf("%d\n", sizeof(float));
	printf("%d\n", sizeof(double));	
	printf("%d\n", sizeof(char));
	printf("%d\n", sizeof(str));
}
#include <Stdio.h>

void main() {
	int age = 10;
	float height = 125.3F;
	double weight = 35.9;
	char grade = 'A';
	char* name = "정대한";
	
	printf("나이 :%d살", age);
	printf("키 : %.1fcm", height); //소수점을 자를 때 6부터 반올림된다.
	printf("몸무게 : %.2lf", weight);
	printf("수행평가 : %c등급", grade);
	printf("이름 : %s", name);
}
